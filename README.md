# odoo-ingest

Incremental data ingestion from Odoo via JSON-RPC into Postgres JSONB.

## Minimal architecture

Architecture and data flow are documented in:

- `docs/minimal-architecture.md`

Base Postgres schema is in:

- `sql/001_init.sql`
